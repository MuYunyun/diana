import { setTimeout } from 'timers';

const _ = require('../../dist/browser/index.js')
const assert = require('assert')

describe('#Http API:', () => {
  describe('#redirect()', () => {
    it(`test redirect`, () => {
      // _.redirect('https://www.baidu.com/', false)
    })
  })
})